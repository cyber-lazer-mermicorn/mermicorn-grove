# Pin surface & scope rules

## Recommended pins (proven only)

1. `mermicorn-grove` — constellation registry
2. `cherry-ravewear-studio` — flagship passion + packages
3. `mermicorn-mega-boot` — real bootstrap CLI
4. `ai-deal-finder` **or** `cherry-travel-deal-lab` (see split below)
5. `mermicorn-commerce-ai` — after P0 hygiene
6. `cherry-operator-apprenticeship` — career ladder

Do **not** pin stubs: `mermicorn-client`, `cherry-portfolio` until they have real surface.

## Product vs lab — travel

| Repo | Role |
|------|------|
| **cherry-travel-deal-lab** | Portfolio lab: methodology, case studies, total-cost frameworks, career evidence |
| **ai-deal-finder** | Product experiment: runnable scorer/searcher app with UI and deploy configs |

Lab owns *how Cherry works a deal*. Product owns *a shippable tool*. Do not duplicate narratives; cross-link only.

## Inventory truth (2026-08-09)

- **16 public + 2 private** repositories
- Private: `mermicorn-memory`, `mermicorn-private-ops`
- New since original 15-plan: `ai-deal-finder`, `mermicorn-client`, `cherry-portfolio`

## Wikis
Prefer disabling empty wikis on public repos (Settings → Features → Wikis off) to reduce empty surface.
